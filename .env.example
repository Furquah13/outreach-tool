# Environment Variables for Outreach Tool

# Server Configuration
PORT=3000

# Tracking Domain Configuration
# Separate domains for click tracking and open tracking (can be the same)
# These should be CNAMEs to your Render Web Service domain
TRACK_LINK_DOMAIN=links.outsetventure.com
TRACK_OPEN_DOMAIN=links.outsetventure.com

# Legacy tracking domain (for backwards compatibility)
TRACKING_DOMAIN=outsetventure.com

# JWT Secret for signing unsubscribe tokens (generate a strong secret)
# You can generate one with: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-here-generate-a-strong-one

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/outreach_tool

# Redis Configuration (for BullMQ job queues)
REDIS_URL=redis://localhost:6379

# Rate Limiting Configuration
# Number of emails to send per minute (default: 80)
RATE_LIMIT_PER_MINUTE=80

# Email Configuration (optional - for future email provider integration)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-password
# FROM_EMAIL=noreply@outsetventure.com
# FROM_NAME="Your Company Name"

# DNS Setup Instructions:
# 1. Create a CNAME record in your DNS:
#    links.outsetventure.com -> your-render-web-service-domain.onrender.com
# 2. In your Render dashboard, add links.outsetventure.com as a custom domain
# 3. Enable HTTPS for the custom domain in Render
